# ng-check-compat-script
A little nodeJS script written in typescript. It crawls through your `package.json`, checks every dependency against your target Angular version, and gives you a nice CSV report with all the juicy details. Think of it as your personal dependency detective!
